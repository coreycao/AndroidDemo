<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="corey.me.helloagera">

  <uses-permission android:name="android.permission.INTERNET"/>

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true">

    <activity android:name="me.demo.rx.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name="me.demo.rx.agera.AgeraActivity"/>
    <activity android:name="me.demo.rx.rxj.RxjActivity1"/>
    <activity android:name="me.demo.rx.rxj.RxjActivity2"/>
    <activity android:name="me.demo.rx.rxj.RxjActivity3"/>
    <activity android:name="me.demo.rx.rxj.RxjActivity4"/>
    <activity android:name="me.demo.rx.rxj.RxjActivity5"/>
    <activity android:name="me.demo.rx.rxj.RxjActivity6"/>

  </application>
</manifest>
